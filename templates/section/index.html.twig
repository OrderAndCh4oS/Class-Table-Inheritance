{% extends 'base.html.twig' %}

{% block body %}
    {% for section in article.sections %}
        {{ section.title }}
        {{ section.orderNumber }}
        {{ section.type }}
    {% endfor %}
{% endblock %}
